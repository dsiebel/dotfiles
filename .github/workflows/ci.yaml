name: 'CI'
'on':
  push: ~
defaults:
  run:
    shell: 'bash'
permissions:
  contents: 'read'
jobs:
  test:
    name: 'Run Tests'
    runs-on: 'ubuntu-latest'
    steps:
      - uses: extractions/setup-just@v2
      - uses: 'actions/checkout@v4'
      - run: |
          just test
