export GOPATH="${HOME}/workspace/source/Go"
export MAKE_PATH="${HOMEBREW_PREFIX}/opt/make/libexec/gnubin"
export KREW_ROOT="${HOME}/.krew"
export KREW_PATH="${KREW_ROOT}/bin"
export GCLOUD_PATH="${HOMEBREW_PREFIX}/Caskroom/google-cloud-sdk/latest/google-cloud-sdk/bin/"
export PATH="${HOME}/bin:${HOME}/.bin:${MAKE_PATH}:${GCLOUD_PATH}:$PATH:$GOPATH/bin:${KREW_PATH}"
